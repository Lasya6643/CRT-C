#include<stdio.h>
int fact(int n){
    if(n==0)
      return 1;
  int result = n * fact(n - 1);
  if(n==5){
    printf("%d\n", result); // Print the factorial result inside the function
    return result;
  }
}
int main(){
    int n;
    scanf("%d",&n);
    fact(n);
    return 0;
}
